post:
  tags:
    - Auth
  operationId: publicAuthPasswordResetRequestCreate
  summary: 创建密码重置请求
  security: []
  parameters:
    - $ref: ../../components/parameters/idempotency_key.yaml
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          additionalProperties: false
          required:
            - email
          properties:
            email:
              type: string
              format: email
  responses:
    "201":
      description: 密码重置请求已受理。
      content:
        application/json:
          schema:
            type: object
            additionalProperties: false
            required:
              - data
              - meta
            properties:
              data:
                type: object
                additionalProperties: false
                required:
                  - id
                  - status
                  - acceptedAt
                properties:
                  id:
                    type: string
                    format: uuid
                  status:
                    type: string
                    enum:
                      - accepted
                  acceptedAt:
                    type: string
                    format: date-time
              meta:
                type: object
                additionalProperties: false
    "400":
      description: 请求格式错误。
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/common/problem.yaml
    "409":
      description: 冲突。
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/common/problem.yaml
    "422":
      description: 业务校验失败。
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/common/validation_problem.yaml
