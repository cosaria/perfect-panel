package config

// Build metadata injected by ldflags during release builds.
var (
	Version     = "unknown version"
	BuildTime   = "unknown time"
	Repository  = "https://github.com/perfect-panel/server"
	ServiceName = "ApiService"
)
